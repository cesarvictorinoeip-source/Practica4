name: Build, Push Docker Image and Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up variables
        id: vars
        run: |
          echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image with tags
        run: |
          docker build -t 20241310/hello-world-devops:latest \
                       -t 20241310/hello-world-devops:${{ env.SHA_SHORT }} \
                       -t 20241310/hello-world-devops:${{ env.DATE }} .

      - name: Push Docker images
        run: |
          docker push 20241310/hello-world-devops:latest
          docker push 20241310/hello-world-devops:${{ env.SHA_SHORT }}
          docker push 20241310/hello-world-devops:${{ env.DATE }}

      - name: Trigger Render deploy
        run: |
          curl -X POST https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json"
